---
name: Dropdown
---

import { Playground, PropsTable } from 'docz'
import Dropdown from '@emcasa/ui-dom/components/Dropdown'
import View from '@emcasa/ui-dom/components/View'
import Row from '@emcasa/ui-dom/components/Row'
import Text from '@emcasa/ui-dom/components/Text'
import Icon from '@emcasa/ui-dom/components/Icon'
import ItunesSearch from './search'

# Dropdown

Dropdown component.

## Basic usage

<Playground style={{minHeight: "250px"}}>
  <Dropdown onChange={(value) => console.log('selected', value)}>
    <Dropdown.Option value="a">Option A</Dropdown.Option>
    <Dropdown.Option value="b">Option B</Dropdown.Option>
    <Dropdown.Option value="c">Option C</Dropdown.Option>
  </Dropdown>
</Playground>

## Placeholder and labels

<Playground style={{minHeight: "250px"}}>
  <View mb="10px">
    <Text>Dropdown with placeholder</Text>
    <Dropdown placeholder="Placeholder">
      <Dropdown.Option value="a">Option A</Dropdown.Option>
      <Dropdown.Option value="b">Option B</Dropdown.Option>
      <Dropdown.Option value="c" label="Label for option C">Option C</Dropdown.Option>
    </Dropdown>
  </View>
  <View mb="10px">
    <Text>Dropdown with fixed label</Text>
    <Dropdown label="Fixed label">
      <Dropdown.Option value="a">Option A</Dropdown.Option>
      <Dropdown.Option value="b">Option B</Dropdown.Option>
      <Dropdown.Option value="c">Option C</Dropdown.Option>
    </Dropdown>
  </View>
</Playground>

## Icons

You can use the `icon` and `iconProps` props on the `Dropdown` component to style the button icon.

<Playground style={{minHeight: "250px"}}>
  <Dropdown icon="heart" iconProps={{color: 'red'}} placeholder="What's your favorite animal?">
    <Dropdown.Option value="cat" label="Cat">
      <Icon name="cat" color="dark" mr="15px" />
      <Text inline>Cat</Text>
    </Dropdown.Option>
    <Dropdown.Option value="dog" label="Dog">
      <Icon name="dog" color="dark" mr="15px" />
      <Text inline>Dog</Text>
    </Dropdown.Option>
    <Dropdown.Option value="kiwi" label="Kiwi">
      <Icon name="kiwi-bird" color="dark" mr="15px" />
      <Text inline>Kiwi</Text>
    </Dropdown.Option>
    <Dropdown.Option value="dragon" label="Dragon">
      <Icon name="dragon" color="dark" mr="15px" />
      <Text inline>Dragon</Text>
    </Dropdown.Option>
  </Dropdown>
</Playground>

Use `flexDirection` to position the dropdown icon and `textAlign` to position the dropdown text.

<Playground style={{minHeight: "250px"}}>
  <View mb="10px">
    <Dropdown flexDirection="row-reverse" placeholder="Select an option">
      <Dropdown.Option value="a">Option A</Dropdown.Option>
      <Dropdown.Option value="b">Option B</Dropdown.Option>
      <Dropdown.Option value="c">Option C</Dropdown.Option>
    </Dropdown>
  </View>
  <View mb="10px">
    <Dropdown flexDirection="row-reverse" textAlign="right" placeholder="Select an option">
      <Dropdown.Option value="a">Option A</Dropdown.Option>
      <Dropdown.Option value="b">Option B</Dropdown.Option>
      <Dropdown.Option value="c">Option C</Dropdown.Option>
    </Dropdown>
  </View>
</Playground>

## Advanced usage

<Playground style={{minHeight: "250px"}}>
  <ItunesSearch>
  {({search, results, onChange}) => (
    <Dropdown
      icon="search"
      height="tall"
      containerProps={{maxHeight: "150px"}}
      label={(
        <ItunesSearch.Input
          value={search}
          onChange={(e) => onChange(e.target.value)} />
      )}>
      {results.length ? (
        results.map(({collectionId, artworkUrl60, artistName, collectionName}) => (
          <Dropdown.Option key={collectionId} value={collectionId}>
            <View mr="15px">
              <img src={artworkUrl60} height="30px" width="30px" />
            </View>
            {artistName} - {collectionName}
          </Dropdown.Option>
        ))
      ) : search.length ? (
        <View mr="15px" ml="15px">
          <Text>No results for "{search}"</Text>
        </View>
      ) : (
        <View mr="15px" ml="15px">
          <Text>Start typing to search albums</Text>
        </View>
      )}
    </Dropdown>
  )}
  </ItunesSearch>
</Playground>

