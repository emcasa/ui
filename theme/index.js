import {
  space,
  color,
  fontSize,
  width,
  fontWeight,
  lineHeight,
} from 'styled-system'

const theme = {
  fontSizes: [
    16, 24, 32
  ],
  space: [
    0, 4, 10, 14, 20
  ],
  colors: {
    blue: '#ff0000',
    red: '#e10',
  }
}

export default theme
